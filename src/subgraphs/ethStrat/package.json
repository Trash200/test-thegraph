{
  "name": "ethStrat",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ trash200/ethStrat",
    "create-local": "graph create --node http://localhost:8020/ trash200/ethStrat",
    "remove-local": "graph remove --node http://localhost:8020/ trash200/ethStrat",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 trash200/ethStrat",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.69.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
